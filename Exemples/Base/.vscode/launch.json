{
    "configurations": [
        {
            "type": "by-gdb",
            "request": "launch",
            "name": "Launch(gdb) BlackJack",
            "program": "BlackJack",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "debug BlackJack"
        }
    ]
}